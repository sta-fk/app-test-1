{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <h1>Edit group {{ group.name }}</h1>
    </div>
    <div class="row">
        <div class="col-5">
            {% include 'group/form.html.twig' with {form: form, group: group} %}
        </div>
    </div>
{% endblock %}